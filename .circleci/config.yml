version: 2
jobs:
  build:
    docker:
      - image: cimg/php:7.2.24
    working_directory: /test
    steps:
      - run:
          name: Creating Dummy Artifacts
          command: |
            echo "my artifact file" > /test/new.text;
            echo ${SSH_PRIVAYE_KEY};

      - run:
          command: |
            zip -r test.zip *
      
      - store_artifacts:
          path: ./test.zip